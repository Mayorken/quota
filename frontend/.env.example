# Local dev needs nothing here — requests go to /api via the Vite proxy.
#
# In production (Vercel), set this to your deployed backend origin so the app
# calls the live API:
# VITE_API_BASE_URL=https://quota-backend.onrender.com
VITE_API_BASE_URL=
